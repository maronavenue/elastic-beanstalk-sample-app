# Ignore NodeJS modules for the meantime to keep it lightweight
node_modules/

# Elastic Beanstalk Files
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml

# Green (Non-prod branch), so ignore prod option settings
.ebextensions/*